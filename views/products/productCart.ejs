<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head'); %> 
<body>
    <%- include('../partials/header'); %> 
    <main>
        <div class="cart">
            <% if(list.length === 0){ %>
                <h2>No hay elementos en el carrito</h2>
            <% } else { %>
                <ul class="product-list">
                    <li>
                        <% list.map( p => { %> 
                            <img src="<%= p.img %>">
                            <span class="nombre-producto"><%= p.name %> </span>
                            <span class="precio-producto">$<%= p.precio  %> </span>
                            <div class="control">
                                <button id="boton-menos" class="dec">-</button>
                                <span id="cantidad" class="cantidad"><%= p.cantidad %></span>
                                <button id="boton-mas" class="inc">+</button>
                                <button class="eliminar">X</button>
                            </div>
                        <% }); %>
                    </li>
                </ul>
            <% } %>
        </div>
    </main>
    <script src="/js/cart2.js"></script>
</body>
</html>